# Divergence Ledger — tbc-arena-lab

Known or suspected differences between (a) this simulator, (b) cmangos-tbc
behavior (bootstrap oracle, models 2.4.3), and (c) the pinned ruleset
(Anniversary TBC 2.5.x). Append-only: never delete an entry; mark it
`RESOLVED(<date>): <how>` instead.

Oracle citations: cmangos-tbc @ commit 009455e (separate checkout
`../cmangos-tbc`), audited 2026-06-10. File paths are under `src/game/`.

| ID | Status | Divergence | Rationale |
|----|--------|------------|-----------|
| D-001 | RESOLVED(2026-06-10): oracle confirmed | Glancing blows excluded vs. player targets (zero-width table range when `is_player_target`). | Oracle hard-excludes glancing vs player-controlled victims: `CanGlanceInCombat`, Entities/Unit.cpp:3256–3259. Sim and oracle agree; the differential harness asserts zero glances. Spec M-001. |
| D-002 | RESOLVED(2026-06-10): oracle confirmed | Crushing blows excluded vs. player targets. | Oracle: crushing requires a creature attacker with skill surplus and a player-controlled victim — `CanCrushInCombat`, Entities/Unit.cpp:3242–3254, chance formula 3471–3483. Spec M-001. |
| D-003 | open | Fidelity target is cmangos-tbc's model of **2.4.3** as bootstrap oracle, while the pinned ruleset is **Anniversary TBC 2.5.x**. | Divergences between those two rulesets are expected and will be cataloged as they are found — not hidden. Each future entry cites which side we follow and why. |
| D-004 | open | Integer flooring at each damage-pipeline stage (AP bonus, armor reduction, crit ×2, block subtraction) vs. oracle float pipeline truncated at stage boundaries (uint32 assignments). | Determinism requires integer authoritative values (CLAUDE.md rule 3). Audit detail: oracle keeps floats through `CalculateDamage`→AP→armor and truncates on each uint32 assignment (Entities/Unit.cpp:2031–2038); per-swing difference ≤1 per stage. Distribution-level comparison tolerates this; `arena_diff` carries the offset explicitly. Specs M-002/M-003/M-004. |
| D-005 | RESOLVED(2026-06-10): equivalent granularity | Attack-table roll granularity per-myriad (10000 buckets) vs oracle. | Oracle's combat die rolls `urand(1, 10000)` (Entities/Unit.cpp:2858) with per-myriad premultiplied chances (`chance_u`, shared/Util/Util.h:93–96, round-to-nearest). Same resolution; our pinned chances are exact pm so rounding never differs for fixtures. Spec M-009. |
| D-006 | RESOLVED(2026-06-10): implemented in M1 | Parry-haste (defender's next swing accelerated after parrying) not implemented in M0. | Implemented for M1 (both units attack) per the oracle's main-hand rule, Entities/Unit.cpp:2277–2315. Spec M-010. Off-hand parry-haste remains dual-wield scope. |
| D-007 | open | Frontal-arc test uses integer Bhaskara-I sine (≤0.2% rel. error); oracle uses float trig (`HasInArc`). | Only the sign of the dot product feeds outcomes; divergence is confined to placements within ~0.2% of the exact shoulder line. Oracle's inclusive arc borders (Entities/Object.cpp:1610) match our ties-count-as-front. M0 scenarios pin positions far from the boundary. Spec M-008. |
| D-008 | RESOLVED(2026-06-10): constant verified, arithmetic adopted | Rage conversion `c(70)` stored as 2747 (×10); attacker rage arithmetic. | Oracle polynomial `0.0091107836·L² + 3.225598133·L + 4.2652911` (Entities/Player.cpp:2336) evaluates to 274.7000 at 70 ⇒ 2747 exact. Adopted the oracle's truncate-hit-factor-then-halve arithmetic (spec M-006). Remaining avoid-rage divergences split into D-011/D-012/D-013. |
| D-009 | open | Weapon skill pinned 350 vs defense 350 in both M0 scenarios, so the skill-delta code paths are unexercised by goldens. | Audit found the crit delta vs players uses LEVEL-CAPPED attacker skill, not actual weapon skill (Entities/Unit.cpp:3957) — adopted (spec M-001). Unit tests cover the delta math synthetically; statistical comparison at nonzero deltas still deferred. |
| D-010 | RESOLVED(2026-06-10): oracle confirmed | No "blocked crit" outcome: block and crit are mutually exclusive single-roll table rows. | Oracle's MELEE_HIT_BLOCK branch never applies the crit multiplier (Entities/Unit.cpp:2141–2182); die sides are exclusive by construction. Spec M-001/M-005. |
| D-011 | open | Oracle grants ATTACKER rage on full misses: `(0/c·7.5 + hf)/2 = hf/2` (≈6 rage at 3.6 speed). We grant none. | The attacker-rage block in `DealDamage` (Entities/Unit.cpp:932–967) has no `cleanDamage->damage > 0` or outcome guard, and `DealMeleeDamage` always calls it (Unit.cpp:2318–2319). Believed emulator artifact — no known 2.4.3 evidence for rage on miss; we keep no-rage-on-miss. `TODO(verify)` vs Anniversary 2.5.x. `arena_diff` reports the oracle behavior as an expected divergence. Spec M-006. |
| D-012 | open | Oracle grants attacker rage on dodge/parry, computed from a DOUBLED clean damage. We grant none. | `cleanDamage` is initialized to post-armor damage (Entities/Unit.cpp:2055) and the dodge/parry branches add `totalDamage` again (Unit.cpp:2121/2133), so the rage basis is 2× post-armor damage. The dodge/parry rage refund is documented as a WotLK 3.0.8 change; doubling looks like a bookkeeping bug. We keep no-rage-on-avoid. Spec M-006. |
| D-013 | open | Oracle computes both sides' rage on blocked swings from post-armor PRE-block damage plus the blocked amount again; we use post-block damage actually dealt. | `cleanDamage = totalDamage` then `+= blocked_amount` (Entities/Unit.cpp:2055, 2179–2180). For full blocks the victim still gains rage from the !damage path (Unit.cpp:1009–1013, block not excluded). Authentic behavior uncertain; ours is the conservative "rage from damage dealt" reading. `TODO(verify)`. Spec M-006. |
| D-014 | open | Weapon damage roll: ours is discrete uniform inclusive `[min, max]`; oracle is continuous `frand(min, max)` truncated to uint32 (≈ uniform on `[min, max-1]`). | Oracle: Entities/Unit.cpp:2917, 2031. Means differ by ~0.5 damage pre-armor; we can roll the exact max, the oracle effectively cannot. Kept for integer determinism. `arena_diff` compares damage means with this offset carried explicitly. Spec M-002. |
| D-015 | RESOLVED(2026-06-10): adopted oracle order | Attack-table order: v1 spec had glancing before block; oracle die order is BLOCK before GLANCE. | Die sides enum, Entities/Unit.h:628–640. Zero-width glancing makes this unobservable vs players, but the enum/table order was aligned to the oracle for future mob support. Spec M-001. |
| D-016 | open | Integer rage arithmetic `(375·D + 5·hf·2747)/2747` vs oracle float `((D/c)·7.5 + hf)/2` then `uint32(×10)`. | Equal on all hand-checked values; float could differ by 1 deci-rage when the product lands within 1 ulp of an integer. Accepted; will surface in trace-level differential comparison if it ever matters. Spec M-006. |
| D-017 | open | Parry-haste 20% window computed as integer `floor(speed_ms/5)` vs oracle float `GetAttackTime * 0.20f` truncated on `setAttackTimer`. | Differs by ≤1 ms only for weapon speeds not divisible by 5; all pinned fixture speeds (3600, 2600) are exact. Spec M-010. |
| D-018 | open | Same-timestamp swing resolution (and therefore same-tick lethal ties) ordered by ascending source entity_id. | Our documented total order (CLAUDE.md rule 3, `event_queue.h`); the oracle's per-object update loop has no exact analogue, and real-server outcomes depend on packet/update timing. Any same-tick kill ambiguity is resolved deterministically in our favor of the lower entity_id. Spec M-007. |
| D-019 | open | Victims gain rage from ability damage taken here (2.5·D/c); the oracle grants NO victim rage for spell damage. | The oracle's victim-rage paths exist only in `DealMeleeDamage` (Entities/Unit.cpp:1305–1310, white swings) and the zero-damage branch (Unit.cpp:1009–1013); `DealSpellDamage` → `DealDamage` with SPELL_DIRECT_DAMAGE never rewards the victim. Retail-TBC victims observably gain rage from ability hits (e.g. being Mortal Struck), so we follow `empirical` retail behavior over the oracle. The differential harness must whitelist this when live-oracle capture lands. Spec M-016. |
| D-020 | open | Yellow damage pipeline applies crit BEFORE armor (per the oracle's spell path); white applies armor before crit. | Spell path: crit at Spells/Spell.cpp:1284–1287, armor at 1295; white path is the reverse (Entities/Unit.cpp:2036, 2114). The orders commute up to integer flooring (≤1 damage); each path is ported faithfully. Specs M-003/M-012, D-004. |
